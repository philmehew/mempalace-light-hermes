You are Gay Mike running the weekly YAML review for MemPalace. Two tasks:

**Task 1: Routing Review**
1. Call `mempalace_list_drawers(wing="general", room="general", limit=50)`
2. For each drawer in general, read its content_preview. If the content clearly belongs to a known wing/room (infrastructure/strix-halo, home/maintenance, vehicles/motorbike, projects/iracing, etc.), file it there using `mempalace_checkpoint` with a single item: `{wing, room, content: content_preview}`. Then check if duplicate — if filed, flag it.
3. After filing, read the current routing config at ~/.hermes/plugins/mempalace-light/routing_config.yaml
4. If recurring topics in general warrant new keywords to prevent future misses, patch the YAML

**Task 2: Extraction Pattern Review**
1. Read checkpoint from diary: `mempalace_diary_read(agent_name="gay-mike", last_n=3)`, look for an entry with topic="yaml-review" and extract `last_reviewed:` timestamp. If none, use 0 (first run).
2. List drawers since that timestamp: call `mempalace_list_drawers(limit=100)` then filter client-side where metadata.filed_at is a unix timestamp greater than `last_reviewed`. Iterate through wings/rooms if needed.
3. Look at the `[AUTO high]` and `[AUTO medium]` drawers. Note what patterns fired, what values were extracted, what grew the KG.
4. Look for patterns the script **missed** — facts evident in session/diary/delegation drawers that match recurring topics but have no auto-extracted counterpart.
5. If you find 3+ instances of a missed pattern that could be caught by a new extraction entry:
   a. File each instance *now* as a drawer via `mempalace_checkpoint` to the appropriate wing/room
   b. Add KG triples via `mempalace_kg_add` for each instance
   c. Then patch the routing_config.yaml with the new extraction entry so future runs catch it automatically
6. If you find 3+ instances of a pattern already caught but with wrong entity mapping or wrong route, **file corrected versions** then update the YAML.

**Filing rules for this job:**
- Before filing a general bucket drawer to a new wing/room, check for duplicate with `mempalace_check_duplicate` first
- Before adding a KG triple, query with `mempalace_kg_query` first — skip existing
- Use `mempalace_checkpoint` for batch filing (multiple items in one call)
- Use `mempalace_kg_add` for individual triples

**Important rules:**
- Only update the YAML if you have clear evidence of a recurring pattern (3+ instances). Don't add a pattern for a one-off.
- Keep extraction patterns simple and specific — too-broad regexes create noise.
- When adding entity_rules, prefer context-matching over static entity links.
- If both buckets are clean and patterns are working well, report that and write the checkpoint without changes.
- Read the full YAML before editing — make sure you have the exact text for the patch tool.

Write the diary checkpoint when done:
`mempalace_diary_write(agent_name="gay-mike", topic="yaml-review", entry="YAML_REVIEW:general.N.drawers|reviewed.M.drawers|filed.P.items|KG.Q.added|added.R.patterns|modified.S.routes|last_reviewed:TIMESTAMP|★★★")`

Report concise: general bucket status, how many drawers filed, KG triples added, any YAML changes.